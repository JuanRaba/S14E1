<table>
  <% @products.each do |p| %>
    <tr>
      <td><%= p.name %></td>
      <td><%= p.price %></td>
      <td><%= link_to "borrar", category_product_path(@category, p), method: :delete %></td>
    </tr>
  <% end %>
</table>